import { z } from "zod";
export const createWorkerSchema=z.object({organizationId:z.string().min(1),actorId:z.string().min(1),employeeNumber:z.string().trim().min(2).max(40),nameAr:z.string().trim().min(2),nameEn:z.string().trim().min(2),workerType:z.enum(["employee","contractor","temporary","intern","consultant","daily_labor"]),departmentId:z.string().min(1),positionId:z.string().min(1),managerId:z.string().optional(),locationId:z.string().min(1),calendarId:z.string().min(1),defaultShiftId:z.string().min(1),userId:z.string().optional(),idempotencyKey:z.string().min(4)});
export const punchSchema=z.object({organizationId:z.string().min(1),actorId:z.string().min(1),workerId:z.string().min(1),occurredAt:z.string().datetime(),type:z.enum(["in","out","break_start","break_end"]),source:z.enum(["manual_authorized","web_demo","csv_preview","external_stub"]),idempotencyKey:z.string().min(4)});
export const periodSchema=z.object({organizationId:z.string().min(1),actorId:z.string().min(1),periodStart:z.string().date(),periodEnd:z.string().date(),timezone:z.string().min(1),idempotencyKey:z.string().min(4)}).refine(x=>x.periodEnd>=x.periodStart,{message:"Period end must be after start"});
