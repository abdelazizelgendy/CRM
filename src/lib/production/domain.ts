import type { JobCardStatus, ProductionOrderStatus, RevisionStatus } from "./types";
export class ProductionStateMachine<T extends string> { constructor(private readonly transitions: Record<T, readonly T[]>) {} assert(from: T, to: T) { if (!this.transitions[from]?.includes(to)) throw new Error(`Illegal production transition: ${from} -> ${to}`); } }
export const revisionStateMachine = new ProductionStateMachine<RevisionStatus>({ draft:["under_review","archived"],under_review:["approved","draft","archived"],approved:["released","superseded"],released:["superseded"],superseded:["archived"],archived:[] });
export const orderStateMachine = new ProductionStateMachine<ProductionOrderStatus>({ draft:["planned","cancelled"],planned:["material_check","cancelled"],material_check:["awaiting_approval","planned","cancelled"],awaiting_approval:["released","planned"],released:["in_progress","on_hold"],in_progress:["on_hold","partially_completed","completed"],on_hold:["in_progress"],partially_completed:["in_progress","completed"],completed:["closed"],closed:[],cancelled:[] });
export const jobCardStateMachine = new ProductionStateMachine<JobCardStatus>({ pending:["ready","cancelled"],ready:["started","blocked","cancelled"],started:["paused","blocked","completed","failed_inspection","rework_required"],paused:["started","blocked"],blocked:["ready","started","cancelled"],failed_inspection:["rework_required"],rework_required:["started","completed"],completed:[],cancelled:[] });
