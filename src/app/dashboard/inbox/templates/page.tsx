import { PageTitle } from "@/components/page-title";
import { createInboxSeed } from "@/lib/inbox/seed";

export default function TemplatesPage(){const templates=createInboxSeed().templates.filter((t)=>t.organizationId==="org-madar-demo");return <><PageTitle title="الردود الجاهزة" description="قوالب آمنة بالعربية والإنجليزية مع متغيرات معتمدة"/><div className="template-grid">{templates.map((template)=><article className="panel template-card" key={template.id}><header><div><h2>{template.nameAr}</h2><small>{template.nameEn}</small></div><span className="demo-badge">/{template.shortcut}</span></header><p>{template.contentAr}</p><p dir="ltr">{template.contentEn}</p><footer><span>{template.category}</span><span>{template.channelCodes.join(" · ")}</span></footer></article>)}</div><section className="panel settings-panel"><h2>المتغيرات الآمنة</h2><p className="muted" data-no-translate>{"{{customer_name}} · {{company_name}} · {{agent_name}} · {{ticket_number}}"}</p><p className="muted">يُعرض المحتوى كنص فقط؛ لا يتم تنفيذ HTML أو JavaScript.</p></section></>}
