# معمارية المرحلة الثامنة

المسار: UI → Project Repository/Application Service → Zod + Permission Decision → Domain State Machines/Calculations → Local Adapter. منطق التبعيات، التقدم، الطاقة، الوقت، EVM، الإغلاق والصلاحيات مركزي داخل `src/lib/projects`. `SupabaseProjectRepository` Stub يرفض التشغيل صراحةً. لا يوجد منطق أعمال مكرر في صفحات App Router.
