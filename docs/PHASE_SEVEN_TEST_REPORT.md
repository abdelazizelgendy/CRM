# تقرير اختبار المرحلة السابعة

## نقطة البداية

القاعدة المنشورة: `agent/phase-six-billing-collections` عند `83926ee`. اختبارات الأساس قبل التعديل: lint ناجح، typecheck ناجح، 119/119 اختبارًا ناجحًا، وProduction Build ناجح.

## النتائج الفعلية

- `npm run lint`: ناجح، 0 أخطاء و0 تحذيرات.
- `npm run typecheck`: ناجح.
- `npm test`: ناجح، 18 ملفًا و145/145 اختبارًا.
- `ALLOW_LOCAL_DATA_IN_PRODUCTION=true npm run build`: ناجح، وظهرت جميع مسارات المرحلة السابعة.
- HTTP Production Smoke: ناجح، 28/28 مسارًا أعاد HTTP 200 ومحتوى غير فارغ، بما فيها التفاصيل والتقارير والطباعة.

اختبارات المرحلة تغطي BigInt والتقريب والقيم الكبيرة والعملات 0/2/3، الحالات، عزل شركتين، IDOR، الصلاحيات وإخفاء الماليات، كشف المورد والفاتورة المكررة، Maker-Checker، الميزانية وOverride، Idempotency، أمر شراء واحد لكل ترسية، منع الاستلام الزائد، فصل المستلم عن المعتمد، 3-Way Match، منع خلط المورد/الشركة/العملة، الاعتراف مرة واحدة، عكس المصروف بحركة مقابلة، حساب الالتزام وActual وETC/EAC والربحية وCSV الآمن وSupabase Stub. اختبارات عدم التراجع للمراحل السابقة ضمن 145 اختبارًا نجحت.

## مؤجل أو غير ممكن في البيئة

- `npm run test:integration`: غير ممكن لغياب `NEXT_PUBLIC_SUPABASE_URL`؛ يحتاج مشروع Supabase اختبار مستقلًا. RLS/RPC لم تختبر.
- Migration 008: عقد فقط، لم يطبق أو يختبر تكامليًا.
- الفحص البصري: غير ممكن في البيئة. ثبتت `agent-browser 0.33.2`، لكن daemon فشل في ربط socket برسالة `Operation not permitted (os error 1)`، ولا يوجد Chrome/Chromium محلي. لذلك لم تعتبر قياسات 1440×900 أو 768×1024 أو 390×844 Passing ولم تنشأ Screenshots. Production Build وHTTP Smoke لا يحلان محل الفحص البصري.
- Supabase Adapter: Stub صريح غير منفذ.
- البريد وWhatsApp وإرسال RFQ ورفع الملفات والدفع والبنك والمخزون وGL/AP: خارج النطاق وتحتاج خدمات أو مراحل مستقلة.
- المراجعة المالية والمشتريات والضريبية: تحتاج مختصين قبل الإنتاج.

الحكم قبل النشر: **قيد التنفيذ — غير جاهزة للاعتماد** حتى اكتمال مراجعة Git والنشر إلى Draft PR وفحص CI، مع بقاء الفحص البصري عائقًا موثقًا.
