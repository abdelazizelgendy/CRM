# Identity → HR handover

`profiles.id` هو User و`organization_members.id` هو Membership. `hr_workers.user_id/membership_id` اختياريان وفريدان داخل الشركة ويخضع ربطهما لصلاحية وMaker-Checker. لا ينشأ Worker تلقائيًا من الدعوة، ولا يلغي termination حساب المستخدم تلقائيًا.
